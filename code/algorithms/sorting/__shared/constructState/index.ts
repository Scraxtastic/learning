import { relative } from './relative';
import { absolute } from './absolute';

/**
 * our reconstructor for states of an un-/sorted array
 */
export const constructState = {
  relative,
  absolute,
};
